const int a:=5;
char b,d;
struct ab {
	int abc;
};
int fun(int a, int b, int c);
int f();
int f(int b)
{
 	int d;
	d := 4;
	return d;
}

int main()
{
	int x := 0,y;
	while((x-1) >= 0)
		if(x == 2)
			y := fun(f(x),x,5) + 1;
	for(x := 0; x < 5; x++)
		f(x);
	return 0;
}

